name: TackleTest-UI CI

on:
  push:
    paths-ignore:
      - 'tkltest/generate/unit/**'
      - 'tkltest/execute/unit/**'
      - 'tkltest/util/unit/**'
      - 'tkltest/tkltest_unit.py'
      - 'doc/**'
      - '**.md'
      - '.github/workflows/tkltest_unit_ci.yml'
      - 'test/unit/**'
  pull_request:
    paths-ignore:
      - 'tkltest/generate/unit/**'
      - 'tkltest/execute/unit/**'
      - 'tkltest/util/unit/**'
      - 'tkltest/tkltest_unit.py'
      - 'doc/**'
      - '**.md'
      - '.github/workflows/tkltest_unit_ci.yml'
      - 'test/unit/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Set up JDK 8
      uses: actions/setup-java@v2
      with:
        java-version: '8'
        distribution: 'adopt'
    - name: Install prerequisites
      run: |
        sudo apt-get -qq update
        sudo apt-get -qq install -y bats libgtk2.0-0 chromium-chromedriver
        bats --version
        chromedriver --version
    - name: Create maven settings.xml
      uses: s4u/maven-settings-action@v2.4.0
      with:
        servers: '[
          {"id": "github", "username": "$GITHUB_ACTOR", "password": "${{ secrets.TKLTEST_PKGS_PAT }}"},
          {"id": "github-sinha108", "username": "$GITHUB_ACTOR", "password": "${{ secrets.TKLTEST_PKGS_PAT }}"}
        ]'
    - name: Download Java libraries
      run: |
        cd lib
        sed -ie "s|GITHUB_USERNAME|$GITHUB_ACTOR|g" settings.xml
        sed -ie "s|GITHUB_TOKEN|${{ secrets.TKLTEST_PKGS_PAT }}|g" settings.xml
        ./download_lib_jars.sh
        cd ..; ls lib/*
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Pull test webapps
      run: |
        git submodule init
        git submodule update
    - name: Run petclinic webapp for testing
      run: |
        cd test/ui/data/webapps/spring-petclinic-rest
        docker run -d -p 9966:9966 --name petclinic-rest springcommunity/spring-petclinic-rest
        cd ../spring-petclinic-angular
        docker build -t spring-petclinic-angular:latest .
        docker run -d -p 8080:8080 --name petclinic-angular spring-petclinic-angular:latest
        curl localhost:8080
    - name: Install tkltest-ui command and run bats tests
      env:
        JAVA_HOME: /opt/hostedtoolcache/Java_Adopt_jdk/8.0.292-1/x64
      run: |
        git submodule init
        git submodule update
        python -m venv venv
        source venv/bin/activate
        pip install .
        which tkltest-ui
        bats test/ui/test_cli.bats
        bats test/ui/test_cli_generate.bats
        deactivate
